this.lbInput = (function(){

  var link = function(scope, element, attrs, controller, transclude) {

    // Look up lb-input-read wire
    var x = 0;
  };

  var directive = {
    link: link,
  };

  return function() {

    return directive;
  };
}());

